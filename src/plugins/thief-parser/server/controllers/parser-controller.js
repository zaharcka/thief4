"use strict";

module.exports = ({ strapi }) => ({
  /*async getAllPages(ctx) {
    const {
      request: { body },
    } = ctx;
    ctx.body = await strapi
      .plugin("thief-parser")
      .service("parseService")
      .parseAllPages(body);
  },*/
});
